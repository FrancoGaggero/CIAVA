---
/**
 * Intro Component
 * Descripción del estudio
 */
import muestra1 from '../assets/images/studio/muestra1.jpeg';
import muestra2 from '../assets/images/studio/muestra2.jpeg';
import muestra3 from '../assets/images/studio/muestra3.jpeg';
import muestra4 from '../assets/images/studio/muestra4.jpeg';
---

<section id="intro" class="bg-white py-16 sm:py-24 md:py-32 lg:py-40 px-4 sm:px-6">
  <div class="container mx-auto max-w-6xl">
    <div class="intro-content text-center">
      <div class="mx-auto max-w-4xl border border-black/5 bg-gray-50/30 px-6 py-10 sm:px-12 sm:py-16 md:px-16 md:py-20 mb-12 sm:mb-16">
        <div class="mb-6 sm:mb-8 inline-block">
          <div class="h-px w-10 sm:w-12 bg-black/20 mx-auto mb-6 sm:mb-8"></div>
          <h2 class="mb-6 sm:mb-8 text-xl sm:text-2xl font-light tracking-[0.2em] text-black md:text-3xl">
            CIAVA STUDIO
          </h2>
          <div class="h-px w-10 sm:w-12 bg-black/20 mx-auto"></div>
        </div>
        <div class="space-y-4 sm:space-y-6 md:space-y-8 text-sm sm:text-base md:text-lg leading-relaxed text-black/80 lg:text-xl font-light max-w-3xl mx-auto">
          <p class="tracking-wide">
            Somos un estudio creativo especializado en producción de contenido visual para marcas.
          </p>
          <p class="tracking-wide">
            Creamos imágenes y videos que comunican identidad, elevan el producto y potencian la presencia.
          </p>
        </div>
      </div>

      <!-- Grid de Producciones -->
      <div>
        <div class="mb-8 sm:mb-10 md:mb-12">
          <h3 class="text-lg sm:text-xl font-light tracking-[0.2em] text-black md:text-2xl mb-2 sm:mb-3">
            NUESTRAS PRODUCCIONES
          </h3>
          <p class="text-xs sm:text-sm font-light text-black/60">Ejemplos de nuestro trabajo</p>
        </div>

        <div class="grid grid-cols-1 gap-4 sm:gap-6 md:grid-cols-2 lg:gap-8">
          <!-- Imagen 1 -->
          <div class="production-item group relative aspect-[4/5] overflow-hidden border border-black/5 bg-gray-50">
            <img 
              src={muestra1.src} 
              alt="Producción 1" 
              class="h-full w-full object-cover transition-all duration-700 group-hover:scale-105 group-hover:opacity-90"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100"></div>
          </div>

          <!-- Imagen 2 -->
          <div class="production-item group relative aspect-[4/5] overflow-hidden border border-black/5 bg-gray-50">
            <img 
              src={muestra2.src} 
              alt="Producción 2" 
              class="h-full w-full object-cover transition-all duration-700 group-hover:scale-105 group-hover:opacity-90"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100"></div>
          </div>

          <!-- Imagen 3 -->
          <div class="production-item group relative aspect-[4/5] overflow-hidden border border-black/5 bg-gray-50">
            <img 
              src={muestra3.src} 
              alt="Producción 3" 
              class="h-full w-full object-cover transition-all duration-700 group-hover:scale-105 group-hover:opacity-90"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100"></div>
          </div>

          <!-- Imagen 4 -->
          <div class="production-item group relative aspect-[4/5] overflow-hidden border border-black/5 bg-gray-50">
            <img 
              src={muestra4.src} 
              alt="Producción 4" 
              class="h-full w-full object-cover transition-all duration-700 group-hover:scale-105 group-hover:opacity-90"
            />
            <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent opacity-0 transition-opacity duration-300 group-hover:opacity-100"></div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import { fadeInUp, staggerReveal } from '../scripts/animations.js';

  document.addEventListener('astro:page-load', () => {
    fadeInUp('.intro-content', 200);
    
    setTimeout(() => {
      staggerReveal('.production-item', 150);
    }, 400);
  });
</script>
